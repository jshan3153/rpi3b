#OpenCV-Python 라즈베리파이에 설치하기

1. OpenCV Installation 
  1.1 pip install
    1.1.1 $sudo pip install update
          intall path : 
    1.1.2 $pip install update
          intall path : 

  1.2 apt install
    1.2.1 $sudo apt-get install python3-opencv
    1.2.2 $apt-get install python3-opencv

2. OpenCV version check
$python3 -c "import cv2; print(cv2.__version__)"
4.5.3
